# Prompt 和上下文工程学习材料

## 0. 先建立一个核心理解

同一个模型，为什么有人用得很稳，有人用得像随机发挥？

原因通常不是模型“智商不一样”，而是输入给模型的任务环境不一样。

一个稳定的模型调用，通常包含：

1. 清楚的角色：模型现在扮演谁。
2. 清楚的任务：模型到底要做什么。
3. 清楚的边界：不能做什么、信息不足怎么办。
4. 清楚的格式：必须输出什么字段。
5. 清楚的示例：什么叫好答案。
6. 清楚的上下文：哪些信息重要，哪些不要塞进去。

Prompt 工程不是写一句神奇咒语，而是把“让模型稳定完成任务”当成工程问题处理。

---

## 1. 指令分层（Instruction Hierarchy）

### 概念

指令分层就是把不同优先级、不同作用的内容放在不同位置。

常见层级：

| 层级 | 作用 | 举例 |
| --- | --- | --- |
| system | 最高层规则，定义模型身份和总原则 | 你是严谨的代码解释助手 |
| developer | 开发者规则，定义任务流程、输出契约、边界 | 必须输出 JSON，不能编造 |
| user | 用户本次具体需求 | 请解释这段函数 |
| tool | 工具返回的事实 | AST 分析结果、测试结果、文件内容 |

### 错误写法

```text
帮我解释这个函数，写得详细一点，最好 JSON，别乱说，有 bug 也说一下。
def average(nums):
    return sum(nums) / len(nums)
```

问题是：身份、任务、格式、边界混在一起，模型容易忽略其中一部分。

### 更好的写法

```text
system:
你是一个严谨的代码解释助手，默认使用简体中文。

developer:
你必须只根据代码回答。
你必须输出 JSON。
字段固定为 summary、inputs、outputs、potential_bugs、test_suggestions。
如果信息不足，写入 limitations，不要猜测。

user:
请解释下面函数：
def average(nums):
    return sum(nums) / len(nums)
```

### 你要记住

越稳定的 Prompt，越不像一句自然语言请求，而像一份任务说明书。

---

## 2. Few-shot（少样本示例）

### 概念

Few-shot 是给模型几个“输入 -> 输出”的示例，让模型模仿你的格式、风格和判断标准。

它解决的问题是：

- 输出结构不稳定
- 语气不稳定
- 判断标准不稳定
- 模型不知道你心里“好答案”长什么样

### Zero-shot 示例

```text
请解释这个函数：
def square(x):
    return x * x
```

模型可能输出：

```text
这个函数用于计算平方。输入 x，返回 x*x。
```

也可能输出 Markdown、表格、JSON，结构不可控。

### Few-shot 示例

```text
示例 1：
输入：
def square(x):
    return x * x

输出：
{
  "summary": "计算输入值 x 的平方。",
  "inputs": [{"name": "x", "role": "被平方的值"}],
  "outputs": ["返回 x * x 的结果"],
  "potential_bugs": [],
  "test_suggestions": ["x=2 时应返回 4", "x=0 时应返回 0"]
}

现在请按同样格式解释：
def add(a, b):
    return a + b
```

模型更容易输出同样结构。

### 示例数量怎么选

通常 2-5 个就够。

推荐覆盖：

1. 普通情况
2. 有潜在 bug 的情况
3. 输入不完整或异常情况

---

## 3. Chain-of-thought 替代方案

### 概念

很多人会写：

```text
请一步一步展示你的完整思考过程。
```

这不推荐。

更好的做法是让模型输出“可检查的中间结果”，而不是隐藏推理过程。

### 替代方式 1：输出分析步骤

```json
{
  "analysis_steps": [
    "识别函数名和参数",
    "查找 return 语句",
    "检查除法、索引访问、空值风险",
    "生成测试建议"
  ]
}
```

### 替代方式 2：输出证据

```json
{
  "potential_bugs": [
    {
      "risk": "空列表会导致除零错误",
      "evidence": "return sum(nums) / len(nums)",
      "suggestion": "计算前检查 nums 是否为空"
    }
  ]
}
```

### 替代方式 3：输出检查清单

```json
{
  "checks": {
    "has_return": true,
    "has_division": true,
    "has_empty_input_risk": true,
    "has_index_access": false
  }
}
```

### 你要记住

不要问模型“你脑子里怎么想的”，要问它“你检查了什么、证据是什么、结论是什么”。

---

## 4. Prompt Template（提示词模板）

### 概念

Prompt Template 就是把 prompt 当成代码管理。

不要在代码里随手拼一大段字符串，而是单独放一个模板文件。

### 简单模板

```text
你是代码解释助手。

请解释下面代码：

```python
{{code}}
```

请输出 JSON，字段包括：
- summary
- inputs
- outputs
- potential_bugs
- test_suggestions
```

程序运行时把 `{{code}}` 替换成用户代码。

### 为什么要模板化

1. 方便版本管理。
2. 方便多人协作。
3. 方便测试输出稳定性。
4. 方便以后切换模型。
5. 方便加入 few-shot 示例。

### 你要记住

Prompt 不是聊天草稿，而是生产系统的一部分。

---

## 5. Context Packing（上下文打包）

### 概念

Context Packing 是决定长上下文里：

- 放什么
- 不放什么
- 以什么顺序放
- 压缩到什么粒度

模型不是“看到越多越好”。上下文太乱，模型反而会失控。

### 推荐顺序

```text
1. 角色和最高规则
2. 输出格式契约
3. 当前任务
4. 工具事实
5. 少量示例
6. 用户输入
```

### 错误上下文

```text
这里有 800 行聊天记录、3 个旧需求、10 段日志、几个过期 bug，还有用户现在的代码。
请解释代码。
```

问题：模型不知道哪个信息最重要。

### 更好的上下文

```text
任务：解释一个 Python 函数。
输出：固定 JSON。
工具事实：
- 函数名：average
- 参数：nums
- 返回表达式：sum(nums) / len(nums)
- 风险：len(nums) 可能为 0

用户代码：
def average(nums):
    return sum(nums) / len(nums)
```

### 你要记住

上下文不是仓库，不能什么都塞。上下文应该像给模型准备的考卷资料。

---

## 6. Output Contract（输出契约）

### 概念

Output Contract 是明确规定模型必须输出什么格式。

比如：

```json
{
  "function_name": "string",
  "summary": "string",
  "inputs": [],
  "outputs": [],
  "potential_bugs": [],
  "test_suggestions": [],
  "limitations": [],
  "confidence": "high | medium | low"
}
```

### 没有输出契约的问题

模型可能今天输出：

```text
这个函数用于计算平均值。
```

明天输出：

```markdown
## 功能
计算平均值

## 风险
空列表会报错
```

后端程序很难解析。

### 有输出契约后

```json
{
  "function_name": "average",
  "summary": "计算 nums 中所有元素的平均值。",
  "inputs": [
    {
      "name": "nums",
      "role": "用于求平均值的数字集合",
      "default": null
    }
  ],
  "outputs": [
    {
      "kind": "return",
      "description": "返回 sum(nums) / len(nums) 的结果。"
    }
  ],
  "potential_bugs": [
    {
      "risk": "当 nums 为空时会触发除零错误。",
      "evidence": "len(nums) 作为除数。",
      "suggestion": "计算前检查 nums 是否为空。"
    }
  ],
  "test_suggestions": [
    {
      "case": "nums=[1, 2, 3]",
      "reason": "验证普通输入。"
    },
    {
      "case": "nums=[]",
      "reason": "验证空列表边界。"
    }
  ],
  "limitations": [],
  "confidence": "high"
}
```

### 你要记住

只要输出要被程序继续使用，就必须设计输出契约。

---

## 7. 把这些技术合在一起

一个稳定的代码解释助手 Prompt 可以这样设计：

```text
system:
你是一个严谨的代码解释助手，默认使用中文。

developer:
你必须只解释用户提供的函数代码。
你必须输出合法 JSON。
字段固定为：
function_name, summary, inputs, outputs, potential_bugs, test_suggestions, limitations, confidence。
潜在 bug 必须包含 evidence。
信息不足时写入 limitations，不要猜测。

few-shot:
示例 1：普通函数
示例 2：有除零风险的函数
示例 3：没有 return 的函数

tool:
静态分析结果：
{
  "function_name": "average",
  "args": ["nums"],
  "returns": ["sum(nums) / len(nums)"],
  "has_division": true
}

user:
def average(nums):
    return sum(nums) / len(nums)
```

这就是 Prompt 和上下文工程的核心：  
不是让模型自由发挥，而是给它角色、规则、示例、事实和格式。

---

## 8. 学习练习

### 练习 1

给下面函数设计输出契约：

```python
def first_item(items):
    return items[0]
```

你应该发现：

- 功能：返回第一个元素
- 输入：items
- 输出：items[0]
- bug：空列表时索引越界
- 测试：普通列表、空列表、单元素列表

### 练习 2

给下面函数写 few-shot 示例：

```python
def divide(a, b):
    return a / b
```

你应该覆盖：

- b 不为 0
- b 为 0
- a 为 0

### 练习 3

把下面混乱 prompt 改成分层 prompt：

```text
你帮我看下这个代码，写得详细点，看看有没有问题，最好 JSON，我后面程序要处理，别太啰嗦。
```

改写方向：

```text
system:
你是代码解释助手。

developer:
输出必须是 JSON。
字段固定为 ...
潜在问题必须给证据。

user:
请解释下面代码 ...
```

---

## 9. 最重要的一句话

Prompt 工程的本质是：  
把“我希望模型懂我”的模糊期待，变成“模型可以稳定执行”的明确协议。
