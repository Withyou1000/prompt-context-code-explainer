# 从“会写 Prompt”到“把 Prompt 做成项目”：我的代码解释助手学习记录

最近在学 Prompt 和上下文工程。刚开始我以为 Prompt 工程就是把一句话写得更漂亮，比如“请你作为资深工程师解释下面代码”。真正做了一个小项目之后，我才发现它更像软件工程：要分层、要模板、要约束输出、要测试稳定性，还要面对真实接口调用里的各种问题。

这篇文章记录我做“代码解释助手”的过程。项目很小，但刚好把几个关键概念串起来了：指令分层、Few-shot、Chain-of-thought 替代方案、Prompt Template、Context Packing 和 Output Contract。

## 一、我一开始想做什么

目标很简单：输入一段函数代码，输出这几个部分：

- 功能说明
- 输入输出
- 潜在 bug
- 测试建议

比如输入：

```python
def average(nums):
    total = sum(nums)
    return total / len(nums)
```

理想输出不是一段散文，而是结构稳定的 JSON：

```json
{
  "function_name": "average",
  "summary": "计算 nums 中所有元素的平均值。",
  "inputs": [
    {
      "name": "nums",
      "role": "用于求平均值的数字集合",
      "default": null
    }
  ],
  "outputs": [
    {
      "kind": "return",
      "description": "返回 total / len(nums) 的计算结果。"
    }
  ],
  "potential_bugs": [
    {
      "risk": "当 nums 为空时可能触发除零错误。",
      "evidence": "代码中 len(nums) 出现在除法表达式中。",
      "suggestion": "计算前检查 nums 是否为空。"
    }
  ],
  "test_suggestions": [
    {
      "case": "nums=[1, 2, 3]",
      "reason": "验证普通输入。"
    },
    {
      "case": "nums=[]",
      "reason": "验证空集合边界。"
    }
  ],
  "limitations": [],
  "confidence": "high"
}
```

这个目标看起来不难，但真正的问题是：同一个输入连续跑 5 次，输出结构还能不能稳定？

## 二、第一个版本：我走了一点弯路

项目一开始做成了本地规则版，用 Python 的 `ast` 模块分析代码。

这个版本能识别：

- 函数名
- 参数
- `return`
- 除法
- `len(...)`
- 下标访问

它当然很稳定，因为没有调用大模型，输出都是代码规则生成的。连续跑 5 次结果肯定一样。

但这也暴露了一个问题：如果我想学 Prompt 工程，只做本地规则分析是不够的。Prompt 模板虽然写了，但没有真正交给模型用，Few-shot、输出契约、上下文排序这些东西都没有被真实检验。

所以后来我把 AST 版本删掉，改成只调用大模型。这样项目才真正进入 Prompt 工程的练习范围。

## 三、指令分层：不要把所有要求塞成一句话

我以前可能会这样写 Prompt：

```text
请解释下面代码，输出 JSON，不要废话，有 bug 也说一下，最好给测试建议。
```

这类 Prompt 的问题是：角色、任务、格式、边界全混在一起。模型有时会听，有时会漏。

后来我把 Prompt 拆成几层：

```text
system:
你是一个严谨、稳定、面向初学者友好的代码解释助手。
你只根据用户提供的代码回答，不编造不存在的上下文。
默认使用简体中文。

developer:
你需要解释一段函数代码，并严格遵守输出契约。
输出必须是合法 JSON，不要输出 Markdown。
潜在 bug 必须有代码证据。

user:
请解释下面函数代码：
{{code}}
```

这样做之后，Prompt 不再像一句“愿望”，而像一份“任务说明书”。

我对指令分层的理解是：

- `system` 放身份、总原则和不可变规则。
- `developer` 放开发者希望模型稳定遵守的执行规范。
- `user` 放这一次真实变化的输入。
- `tool` 放工具返回的事实，比如检索结果、测试结果、静态分析结果。

这个项目目前只保留了用户代码，没有再放工具事实，因为我已经把 AST 分析删掉了。

## 四、Few-shot：不要只说规则，还要给标准答案

Few-shot 的作用是给模型几个“输入 -> 输出”的例子，让它知道什么叫好答案。

在代码解释助手里，我放了两个例子：

第一个是普通函数：

```python
def square(x):
    return x * x
```

这个例子告诉模型：没有明显 bug 时，`potential_bugs` 可以是空数组。

第二个是有风险的函数：

```python
def average(nums):
    return sum(nums) / len(nums)
```

这个例子告诉模型：发现风险时，不要只写“可能有 bug”，还要写：

- `risk`：风险是什么
- `evidence`：证据来自哪段代码
- `suggestion`：应该怎么改

我现在对 Few-shot 的理解是：它不是为了让模型背答案，而是为了统一输出风格和判断标准。

## 五、Chain-of-thought 替代方案：不要追问模型脑子里怎么想

一开始我也会想让模型“逐步思考”。但实际做项目时，我更需要的是稳定、可检查、能被程序处理的结果。

所以我没有要求模型输出完整思考过程，而是要求它输出这些字段：

```json
{
  "potential_bugs": [
    {
      "risk": "风险描述",
      "evidence": "代码证据",
      "suggestion": "修改建议"
    }
  ]
}
```

这就是 Chain-of-thought 的替代思路。

不要问模型：

```text
请展示你的完整推理过程。
```

而是问它：

```text
请给出检查项、证据和结论。
```

这样输出更短、更稳定，也更适合放进程序里继续处理。

## 六、Prompt Template：把 Prompt 当成代码管理

这个项目里，Prompt 没有直接写死在 Python 字符串里，而是拆成文件管理：

```text
prompts/system.zh.md
prompts/code_explainer.zh.md
```

`system.zh.md` 放真正作为 system 消息发送的系统提示词。

`code_explainer.zh.md` 放任务规则、输出契约、Few-shot 示例和用户代码占位符。

里面有一个占位符：

```text
{{code}}
```

程序运行时会读取模板，然后把用户代码填进去：

```python
def build_prompt(code: str) -> str:
    return (
        "## system\n\n"
        f"{read_system_prompt()}\n\n"
        "## user\n\n"
        f"{build_user_prompt(code)}"
    )
```

这一步让我意识到：Prompt 不是聊天草稿，而是项目资产。它应该像代码一样被保存、修改、版本管理和测试。

当 Prompt 单独成文件后，有几个好处：

- 可以清楚看到 system、developer、few-shot、user 分别写了什么。
- 改 Prompt 不需要翻业务代码。
- 可以用 `--show-prompt` 查看最终组装结果。
- 可以配合 5 次连续输出，直接观察模型的结构和措辞是否漂移。

## 七、Context Packing：上下文不是越多越好

Context Packing 是我觉得最容易被忽略的点。

大模型上下文很长，但不代表什么都应该塞进去。项目里最终 Prompt 的顺序是：

```text
1. system 规则
2. developer 输出契约
3. few-shot 示例
4. user 代码
```

这个顺序的意思是：先告诉模型“你是谁、必须怎么输出”，再给它示例，最后给它本次任务。

我现在会刻意避免把这些东西塞进 Prompt：

- 无关聊天记录
- 过长日志
- 重复代码
- 已经过期的需求
- 和当前任务无关的解释

上下文不是仓库，更像考场资料。给得越准确，模型越不容易跑偏。

## 八、Output Contract：没有契约，后面就没法测试

如果只让模型“解释代码”，它可能输出 Markdown，也可能输出自然语言段落。人能看懂，但程序不好处理。

所以我给模型规定了固定 JSON 结构：

```json
{
  "function_name": "string",
  "summary": "string",
  "inputs": [],
  "outputs": [],
  "potential_bugs": [],
  "test_suggestions": [],
  "limitations": [],
  "confidence": "high | medium | low"
}
```

这就是 Output Contract。

它的意义不是让答案看起来“规整”，而是让后续可以做这些事：

- 判断字段是否缺失
- 连续跑 5 次直接对比结构和措辞变化
- 把结果接到前端页面
- 把 bug 和测试建议单独提取出来

只要输出要被程序继续使用，就应该先设计输出契约。

## 九、真正接大模型后，才遇到真实问题

项目改成真实调用大模型后，运行流程变成：

```text
读取 Python 函数文件
-> 读取 Prompt 模板
-> 替换 {{code}}
-> 读取 .env 里的 API 配置
-> 调用 OpenAI 兼容接口
-> 解析模型返回的 JSON
-> 打印结果
```

`.env` 示例：

```env
OPENAI_API_KEY=你的 API Key
OPENAI_BASE_URL=https://ai.12zx.net/v1
OPENAI_API_MODE=chat
OPENAI_MODEL=gpt-5.4
OPENAI_USER_AGENT=OpenAI/Python 1.0.0
```

真实调用时我还遇到了 Cloudflare 403：

```text
Error 1010: Access denied
browser_signature_banned
```

这说明项目代码能发请求，但接口服务可能会拦截脚本调用。这也是接入真实模型后才会遇到的问题：Prompt 写得再好，API 网关、鉴权、请求头、服务商策略也都可能影响结果。

## 十、我最后得到的项目形态

现在这个项目保留了几个关键文件：

```text
prompts/system.zh.md
prompts/code_explainer.zh.md
src/code_explainer/prompting.py
src/code_explainer/llm_client.py
src/code_explainer/cli.py
tests/stability_check.py
```

它们分别对应：

- Prompt 模板
- Prompt 组装
- 大模型调用
- 命令行入口
- 连续 5 次输出对比

运行解释：

```powershell
uv run python -m src.code_explainer.cli examples/sample_function.py
```

查看最终 Prompt：

```powershell
uv run python -m src.code_explainer.cli examples/sample_function.py --show-prompt
```

连续调用 5 次并直接对比输出：

```powershell
uv run python tests/stability_check.py examples/sample_function.py
```

## 十一、这次学习最大的收获

以前我以为 Prompt 工程是“会不会写提示词”。现在我更愿意把它理解成“围绕模型调用做工程化约束”。

这几个概念最后在项目里对应得很清楚：

- 指令分层：不同规则放到不同层级，不混在一句话里。
- Few-shot：用示例固定输出风格和判断标准。
- Chain-of-thought 替代方案：不要暴露完整思考过程，而是输出证据、检查项和结论。
- Prompt Template：把 Prompt 放进文件，像代码一样管理。
- Context Packing：只放必要上下文，并控制顺序。
- Output Contract：让模型输出可以被程序解析的固定结构。

最重要的一句话是：

```text
Prompt 工程不是写一句更聪明的话，而是把模型调用设计成一个可维护、可测试、可迭代的小系统。
```
